const initialState = {
  errors: "",
  isLoading: "",
  isAuth: false,
};

const reducers = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default reducers;
